# =============================================================================
# modules/nixos/desktop/ssh.nix — 桌面 SSH 覆盖配置
# -----------------------------------------------------------------------------
# 桌面主机允许 X11 转发（GUI 程序远程显示）。
# =============================================================================
{ lib, ... }:
{
  services.openssh.settings = {
    # 桌面主机允许 X11 转发
    X11Forwarding = lib.mkForce true;
  };
}
