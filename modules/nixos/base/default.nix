# =============================================================================
# modules/nixos/base/default.nix — 基础系统模块入口
# -----------------------------------------------------------------------------
# 自动导入当前目录下所有 .nix 文件和子目录（排除 default.nix）。
# =============================================================================
{ mylib, ... }:
{
  imports = mylib.scanPaths ./.;
}
