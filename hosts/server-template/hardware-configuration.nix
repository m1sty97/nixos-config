# =============================================================================
# hosts/server-template/hardware-configuration.nix — 硬件相关配置（模板）
# -----------------------------------------------------------------------------
# 磁盘分区与文件系统由 disko 声明式管理（见同目录 disko.nix），
# 挂载点与交换分区由 disko 自动生成，此处不要重复定义。
#
# 作为 QEMU/KVM 虚拟机运行时，initrd 内核模块由 qemu-guest profile
# 自动处理，通常无需手动维护；物理机或特殊硬件部署时，
# 运行 `sudo nixos-generate-config` 并将内核模块部分合并到本文件。
# =============================================================================
{ ... }:
{ }
