# =============================================================================
# home/base/home.nix — Home Manager 基础配置
# -----------------------------------------------------------------------------
# 设置用户名和 Home Manager 状态版本。
# =============================================================================
{ myvars, ... }:
{
  home = {
    inherit (myvars) username;

    # Home Manager 状态版本（不要随意修改）
    stateVersion = "26.05";
  };
}
