# =============================================================================
# home/base/gui/vscode.nix — VS Code 图形编辑器
# -----------------------------------------------------------------------------
# 桌面主机的图形化编辑器（仅桌面主机，服务器无 GUI 不安装）。
# 主题由 catppuccin 模块（home/base/core/theme.nix）自动应用。
# =============================================================================
{ ... }:
{
  programs.vscode = {
    enable = true;
  };
}
