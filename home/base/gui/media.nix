# =============================================================================
# home/base/gui/media.nix — 媒体工具
# -----------------------------------------------------------------------------
# 音频控制、视频播放、图片查看等桌面媒体工具。
# =============================================================================
{ pkgs, ... }:
{
  home.packages = with pkgs; [
    # 音频控制
    pavucontrol # PulseAudio 音量控制 GUI
    playerctl # 媒体播放控制
    pulsemixer # 终端音频混音器

    # 图片查看
    imv # 轻量级 Wayland 图片查看器

    # 视频/音频工具
    libva-utils # VA-API 硬件加速检测
    vdpauinfo # VDPAU 信息

    # 录屏工具
    wf-recorder # Wayland 屏幕录制
  ];

  # ---------------------------------------------------------------------------
  # mpv — 视频播放器
  # ---------------------------------------------------------------------------
  programs.mpv = {
    enable = true;
    defaultProfiles = [ "gpu-hq" ]; # 高质量 GPU 渲染
    scripts = [ pkgs.mpvScripts.mpris ]; # MPRIS 媒体控制集成
  };

  # ---------------------------------------------------------------------------
  # playerctld — 媒体播放守护进程
  # ---------------------------------------------------------------------------
  services.playerctld.enable = true;
}
