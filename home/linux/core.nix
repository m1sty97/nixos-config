# =============================================================================
# home/linux/core.nix — Linux 核心（无 GUI）用户配置
# -----------------------------------------------------------------------------
# 适用于服务器主机（无桌面环境）或桌面主机的核心 CLI 部分。
# 导入 base/core + base/home + linux/base。
# =============================================================================
{
  imports = [
    ../base/core
    ../base/home.nix
    ./base
  ];
}
