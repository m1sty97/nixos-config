// =============================================================================
// niri/conf/windowrules.kdl — Niri 窗口规则
// -----------------------------------------------------------------------------
// 定义特定应用的窗口行为（打开方式、尺寸等）。
// 文档：https://yalter.github.io/niri/Configuration:-Window-Rules
// =============================================================================

// -----------------------------------------------------------------------------
// 对话框/弹窗：以浮动方式打开
// -----------------------------------------------------------------------------
window-rule {
    match app-id=".*dialog.*"
    open-floating true
}

window-rule {
    match app-id=".*popup.*"
    open-floating true
}

// -----------------------------------------------------------------------------
// 文件选择器：浮动
// -----------------------------------------------------------------------------
window-rule {
    match app-id=".*file-chooser.*"
    open-floating true
}

// -----------------------------------------------------------------------------
// Firefox 隐私窗口：不带边框
// -----------------------------------------------------------------------------
window-rule {
    match title=".*Private Browsing.*"
    draw-border-with-background false
}

// -----------------------------------------------------------------------------
// 锁屏：阻止关闭
// -----------------------------------------------------------------------------
window-rule {
    match app-id="swaylock"
    block-out-from "screen-capture"
}

// -----------------------------------------------------------------------------
// Caelestia Shell：标记为不参与窗口管理
// -----------------------------------------------------------------------------
window-rule {
    match app-id="caelestia-shell"
    // shell 窗口不显示在任务栏
    exclude-from-focus-ring
}
