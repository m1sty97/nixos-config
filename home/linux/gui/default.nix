# =============================================================================
# home/linux/gui/default.nix — Linux GUI 配置入口
# -----------------------------------------------------------------------------
# 自动导入当前目录下所有 .nix 文件和子目录。
# 注意：niri.nix、noctalia.nix 和 hyprland.nix 也在扫描范围内，
# 但它们内部使用 mkIf 条件控制，只有在对应选项启用时才生效。
# =============================================================================
{ mylib, ... }:
{
  imports = mylib.scanPaths ./.;
}
