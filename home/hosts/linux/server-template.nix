# =============================================================================
# home/hosts/linux/server-template.nix — 服务器 home 入口模板
# -----------------------------------------------------------------------------
# 新增服务器时复制本文件为 <你的主机名>.nix（无需其他改动——
# 仅导入核心 CLI 配置，无 GUI），并在 outputs/default.nix 的
# 对应 nixosConfigurations 条目中引用它。
# 需要为主机追加用户级配置时，直接在下方 imports / config 区扩展。
# =============================================================================
{
  imports = [ ../../linux/core.nix ];

  # ---------------------------------------------------------------------------
  # 主机特有的 home 配置可在此添加
  # ---------------------------------------------------------------------------
}
