# =============================================================================
# home/hosts/linux/misty-desktop.nix — 桌面主机 Home Manager 配置（Niri 分支）
# -----------------------------------------------------------------------------
# 桌面主机导入完整的 GUI 配置，启用 Niri + Noctalia Shell。
# =============================================================================
{ config, ... }:
{
  imports = [ ../../linux/gui.nix ];

  # ---------------------------------------------------------------------------
  # 启用 Niri 窗口管理器
  # ---------------------------------------------------------------------------
  modules.desktop.niri.enable = true;
}
