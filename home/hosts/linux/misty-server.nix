# =============================================================================
# home/hosts/linux/misty-server.nix — 服务器主机 Home Manager 配置
# -----------------------------------------------------------------------------
# 服务器主机仅导入核心 CLI 配置（无 GUI）。
# hermes CLI 由系统级服务以 addToSystemPackages 全局提供（共享服务状态），
# 用户级无需任何 hermes 配置。
# =============================================================================
{
  imports = [ ../../linux/core.nix ];

  # ---------------------------------------------------------------------------
  # 主机特有的 home 配置可在此添加
  # ---------------------------------------------------------------------------
}
